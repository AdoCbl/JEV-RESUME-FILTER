# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
*.whl
*.so

# uv
.venv/
.uv/
# uv.lock is committed so two machines get the same dependencies

# env / secrets
.env
.env.*
!.env.example
secrets.toml

# Run artifacts. A round file holds the draft and the reviewer's line-by-line verdicts, so
# this directory can contain a real resume and must never be committed.
runs/
UNRESOLVED.md

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Type checking
.mypy_cache/
.pyright/

# IDE
.vscode/
.idea/

# macOS
.DS_Store

# ruff
.ruff_cache/
